# Space Quest

Personal software engineering portfolio. Phase 1 is a simple multi-page React site with placeholder content.

## Run locally

```bash
npm install
npm run dev
```

Then open the URL printed in the terminal (usually `http://localhost:5173`).
